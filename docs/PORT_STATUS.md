# Port Status

The one living tracking doc for the Electron → Rust/Tauri rewrite. See
`RUST-REWRITE-PLAN.md` at the repo root for phase scope/ordering and the
full rationale behind each decision below. Update a row's status the moment
its corresponding Rust module lands and its ported spec (if any) passes —
don't batch updates to the end of a phase.

**Scope change, mid-Phase-1:** the user's actual files are DXF with
meaningful layers (cut/etch/drill/etc.), not SVG. Import/export is now DXF
only, native (the `dxf` crate), with layer identity preserved end-to-end and
DXF export added as new scope the Electron app never had. SVG import
(`svgparser.js`/`domparser.ts`) is dropped from this project entirely — see
the "Dropped from scope" section below instead of "not started." Hole/
interior-feature nesting (e.g. drilled holes) stays fully in scope — nothing
about hole handling was simplified away, only the file format changed.

## Known gotchas to preserve (do not silently "fix" away during the port)

- Rotation-angle-grid quirk: `rotations=6` produces bad angles for
  rectangular parts (confirmed via a 60-run empirical sweep this session) —
  keep user-facing as config, add a warning for non-90-degree-friendly
  rotation counts (Phase 4).
- The `>=` vs `>` rotation-normalization boundary in NFP cache keys.
- `Arotation:0` hardcoded in inner-NFP cache keys.
- Elitist `population[0]` always kept in the GA.
- The mutation-rate cap on rotation-reroll specifically (not order-mutation)
  — fixed an NFP-cache-thrashing bug found this session.
- The NaN-fitness gap in `placeParts` scoring (`fitness += minwidth/sheetarea
  + minarea` only assigned in the ≥2-parts branch) — Phase 3 must make an
  explicit, documented scoring decision here, not paper over it.
- `geometryutil.js`'s `_onSegment` vertical-line branch calls
  `Math.max(B.y, A.y, tolerance)`/`Math.min(B.y, A.y, tolerance)` — a real
  3-argument max/min that includes `tolerance` (~1e-9) as a competing value,
  unlike the horizontal-line branch's plain 2-argument
  `Math.max(B.x, A.x)`/`Math.min(B.x, A.x)`. This asymmetry looks like a typo
  but changes behavior (e.g. `min(5, 10, 1e-9) = 1e-9`, not `5`) and is
  preserved exactly in `geometry::polygon::on_segment`.
- `polygonHull`'s backward vertex scan compares
  `_almostEqual(A[current].y, B[j].y+Boffsety)` — missing `+Aoffsety` on the
  A side, unlike the otherwise-identical forward scan just above it. Almost
  certainly an unintentional asymmetry in the original; preserved exactly in
  `geometry::nfp::polygon_hull` (see the `NB:` comment at that call site).
- `noFitPolygon`'s per-call `marked`-reset loops start at index 1, not 0
  (`for (i=1; i<A.length; i++) A[i].marked=false`) — index 0's `marked` flag
  is never reset by this function, only ever set. Preserved exactly in
  `geometry::nfp::no_fit_polygon`.

## Phase 0 — Scaffolding

| Item | Status | Notes / gotchas |
|---|---|---|
| Cargo workspace (`Cargo.toml`, `crates/geometry`, `crates/nesting`) | done | two-crate split per plan; geometry = pure/testable, nesting = stateful/concurrent |
| Clipper2 Rust FFI binding builds/links on Windows | done | `clipper2` crate (wraps `clipper2c-sys`, C++17 via MSVC) added to `geometry`; smoke-tested with a real boolean union |
| Bare Tauri shell serving copied frontend | done | Tauri v2 shell (`src-tauri/`), `frontendDist` points at `frontend/`, zero commands. Launched and screenshotted: static CSS/nav/icons render correctly; main content area is blank as expected since Ractive population depends on `window.DeepNest`, which never constructs — see Phase 6 note re: `require("electron")` in `index.html` |
| `docs/PORT_STATUS.md` seeded | done | this file |

## Phase 1 — Geometry core

| Electron file/function | Rust module | Status | Notes / gotchas |
|---|---|---|---|
| `main/util/point.ts` | `geometry::point::Point` | done | `marked: bool` added directly on `Point` (matches the JS field, used by NFP tracing) instead of a wrapper type |
| `main/util/vector.ts` | — | not started, deferred | not a dependency of anything ported so far (geometryutil.js uses plain `{x,y}` pairs, not the `Vector` class) |
| `main/util/matrix.ts` | — | dropped, see scope change | was deferred pending SVG-transform-string parsing; SVG import is now out of scope entirely, so this has no remaining consumer. DXF entities carry their own transforms (INSERT/block transforms) — revisit only if block-reference support is needed |
| `main/util/clipper.js` (Clipper1, JS port) | `geometry::clipper` (via `clipper2` crate) | done | upgrade from Clipper1 → Clipper2, not a literal port |
| Offset/boolean ops, `SimplifyPolygon`+`CleanPolygon`, `Area` | `geometry::clipper` | done | Ported the two real composed functions from `main/deepnest.js` exactly, not just raw Clipper2 primitives: `offset()` = `polygonOffset` (miter join, miter limit 4, closed-polygon end — matches the app's exact params, not Clipper2's own defaults); `clean_polygon()` = `cleanPolygon` (self-union with `FillRule::NonZero` to resolve self-intersections — Clipper2's modern equivalent of Clipper1's `SimplifyPolygon` — keep only the largest-area lobe, then `clipper2::simplify` at `0.01 * curve_tolerance` — Clipper2's equivalent of Clipper1's `CleanPolygon` — then drop a duplicated closing endpoint). Also wired general `union`/`intersection`/`difference`/`xor` for Phase 2/3's NFP pipeline. `Area` wasn't re-wrapped — `polygon::polygon_area` (already ported) works on any point list regardless of source. **Real precision decision, not inherited from the crate:** defined a custom `DeepnestScale` `PointScaler` (×10^7) instead of accepting `clipper2`'s own default (`Centi`, ×100) — the Electron app explicitly scales by 10^7 before calling Clipper1 ("ensures integer precision... while avoiding overflow"), and silently accepting the crate's 100x default here would have been a real, easy-to-miss precision regression |
| Custom RDP-simplify post-process (offset-shell re-merge, exterior-point reversal, axis straightening, `.exact` marking) | `geometry::simplify_polygon` | done | This is `main/deepnest.js`'s `simplifyPolygon` method (found the "bigger pipeline" this row was waiting on) — clean → DP-simplify (reuses `geometry::simplify::simplify`, note the pipeline's actual DP call in the Electron app goes through the external `@deepnest/svg-preprocessor` npm package, not `main/util/simplify.js` directly — but that package's `simplifyPolygon(points, tolerance, highQuality)` has the identical signature/algorithm, so the already-ported `main/util/simplify.js` port is the correct reference) → clean → offset (`geometry::clipper::offset`) → `.exact`-point marking → offset-shell reversal against `simple`/fallback shells → axis straightening → offset-shell re-merge (self-union with the original polygon via `geometry::clipper::union_polygons`, keep the most-negative-signed-area loop) → re-clean. Validated against real complex geometry, not just synthetic shapes: all 99 real cut-profile polygons from `tests/fixtures/FLAT.dxf` run through the full pipeline without panicking, each staying within 0.5x-1.5x of its original area (`crates/geometry/tests/dxf_fixtures.rs`). Two disclosed, non-bit-for-bit divergences (documented in the module's doc comment, judged not load-bearing): the `.exact`-marking adjacency check treats "vertex not found in original" as never-adjacent instead of replicating a JS `null`-coerces-to-`0` arithmetic quirk in that edge case; the axis-straightening inner loop drops a `sqds` recheck that's provably dead code in the original (recomputes the same value an outer check already passed, so it can never fire) |
| `main/util/HullPolygon.ts` (`hull()` only) | `geometry::hull_polygon` | done | Andrew's monotone-chain convex hull (ported from d3-polygon, same as the original). Only `hull()` ported — `area`/`centroid`/`contains`/`length` exist on the original class but have zero call sites anywhere in the Electron repo |
| **DXF import** (new scope, not a port — replaces SVG import): entities → polygon tree, layer tag preserved per polygon, parent/hole detection via containment, `.isCircle` metadata for `CIRCLE`/full-sweep `ARC`, oversized-part bbox check | `geometry::dxf_import` (via the `dxf` crate) | mostly done | supports `LWPOLYLINE` (closed, incl. bulge/arc segments via exact tangent-half-angle tessellation), `CIRCLE`, full-sweep `ARC`; parent/hole tree via largest-to-smallest containment (`build_polygon_tree`); `is_oversized` bbox check. Validated against the real `tests/fixtures/FLAT.dxf` fixture (copied from the Electron repo's benchmark assets): 3178 flat polygons (99 `LWPOLYLINE` profiles + 3079 `CIRCLE` drill holes on a `drilling` layer) → tree-built in 36ms release, 99 roots, every circle nested as a hole under its parent profile. **Not yet supported** (deliberately deferred, not attempted half-correct): bare `LINE`/partial-`ARC` networks that only form a closed profile once endpoints are chained together (needs a separate edge-graph-joining algorithm — the fixture above has only 3 bare `LINE` entities, likely non-profile annotation, so this hasn't blocked real-file validation yet); the older heavyweight `POLYLINE` entity (vertices as separate linked sub-entities); `INSERT`/block-reference expansion |
| **DXF export** (new scope, Electron app never had this): write nested layout back out as DXF, reproducing each part's original layer | `geometry::dxf_export` or `nesting`/`src-tauri` (TBD when reached) | not started | Phase 7 scope, noted here since it's directly coupled to the import format decision |
| `main/util/geometryutil.js`: `noFitPolygon`, `noFitPolygonRectangle`, slide/projection distance, search-start-point, polygon hull | `geometry::nfp` | done | ported with unit tests (`crates/geometry/src/nfp.rs`); also ported the transitive dependencies `intersect`, `segmentDistance`, `pointDistance`, `pointInPolygon`, `onSegment`, `lineIntersect`, `almostEqual`, `polygonArea`, `getPolygonBounds`, `isRectangle`, `rotatePolygon` (`crates/geometry/src/polygon.rs`). Preserved quirks: `pointInPolygon`'s bolted-on-offset semantics reproduced via explicit `Point` offset params instead of JS's mutable array properties; `onSegment`'s asymmetric `Math.max(B,A,tolerance)` (vertical branch includes tolerance as a 3rd max/min candidate) vs `Math.max(B,A)` (horizontal branch, no tolerance) preserved exactly; `polygonHull`'s backward-scan y-comparison missing `+Aoffsety` (asymmetric vs. the forward scan) preserved exactly; `noFitPolygon`'s `marked` reset loops starting at index 1 (never resetting index 0) preserved. Deliberately **not** ported yet: `pointLineDistance`, `polygonEdge` — not a dependency of anything Phase 1 requires; deferred to Phase 3 (their only callers are placement-scoring code in `background.js`) |
| `main/util/verifyCircularHoleNfp.js` (disk-fit math only) | `geometry::circular_nfp` | done | ported `fastFitDisk`/`fitsAt` and all 4 brute-force `check()` cases as Rust tests (`crates/geometry/src/circular_nfp.rs`). The *production* `getInnerNfp` circular-hole fast path this verifies (`background.js:1886-1894`) is now wired up too — see Phase 2's `geometry::inner_nfp` row |
| `tests/geometry.spec.ts` | `geometry` unit tests | done | ported: `polygonArea`, `getPolygonBounds`, `almostEqual`, `polygonHull` (all 3 cases), `simplify` (both cases), and the `verifyCircularHoleNfp` self-check (as 4 separate Rust tests instead of one script-style run, since Rust has no direct equivalent of "require and let it throw") |

## Phase 2 — NFP engine

| Electron file/function | Rust module | Status | Notes / gotchas |
|---|---|---|---|
| Outer NFP via Minkowski sum | `geometry::clipper::outer_nfp` | done | Ported `background.js`'s outer/collision-NFP worker (`process` function under the "No-Fit Polygon" comment). Uses Clipper2's built-in `minkowski_diff(b, a, true)` directly instead of the old app's manual "negate B's coordinates, then MinkowskiSum" workaround (Clipper1 had no diff primitive; Clipper2 does) — a real simplification, not just a port. Keeps the most-negative-signed-area loop (same convention `simplify_polygon`/`inner_nfp` use), then translates by `b[0]` since Minkowski math is computed in B's local frame. Tested against two axis-aligned squares (verifies the exact expected NFP bounds) and a B-reference-point-offset case |
| NFP cache-key format (was duplicated between `main/nfpDb.ts`'s `NfpCache.makeKey`/`normalizeRotation` and `main.js`'s `nfpCacheKey`/`normalizeNfpRotation`, kept manually in sync via a code comment) | `nesting::cache_key` | done | One `nfp_cache_key(a, b, a_rotation, b_rotation, a_flipped, b_flipped)` function, byte-for-byte matching both original key formats (`A-B-Arotation-Brotation-Aflipped-Bflipped`) and the `((n%360)+360)%360` rotation normalization (handles negatives correctly, unlike a plain `%360`). Found where the plan's "`>=` vs `>` rotation-normalization boundary" gotcha actually lives: **not** in this key function (its modulo formula already folds any integer correctly) but in `background.js`'s rotation-increment loop (`if (part.rotation >= 360) part.rotation %= 360`) - that's Phase 3/4 scope (placement/GA rotation cycling), noted here so it isn't lost. Also documented the caller convention that must be preserved once this is wired into the real cache (Phase 4/5): `getInnerNfp` always hardcodes `a_rotation: 0` for inner-NFP lookups, since the container doesn't rotate in that scenario. `a_flipped`/`b_flipped` are real parameters (part of the on-wire key format) even though grep confirms nothing in the Electron repo ever actually sets them `true` - no mirrored-part feature exists yet |
| Inner NFP dispatch (`getInnerNfp`'s three-fast-path priority chain: circular-hole exact disk math, rectangular-container fast path, general fallback) | `geometry::inner_nfp::inner_nfp` | done | **The general fallback is "the one piece of the whole project with no existing correct reference to copy"** (per the plan) — confirmed why: the Electron app's own general-case path doesn't use a pure-JS algorithm at all, it wraps container `A` in an artificial "frame" polygon (`getFrame`, `A` becomes the frame's hole) purely to route the request through `addon.calculateNFP`, the confirmed-buggy native addon. This port does **not** replicate that frame trick — `geometry::nfp::no_fit_polygon(inside=true)` (already faithfully ported from `geometryutil.js` in Phase 1) directly supports orbiting `b` inside `a`'s own outer boundary, which is the real algorithm the frame-trick-plus-addon was standing in for. Composed with `outer_nfp` (Minkowski) to subtract each of `a`'s holes as a collision obstacle, matching `getInnerNfp`'s documented 4-step algorithm (frame NFP minus hole NFPs) without the frame indirection. The two existing fast paths from Phase 1 (`fast_fit_disk`/circular, `no_fit_polygon_rectangle`) are wired in ahead of it in the same priority order as the original, including the original's area-based (not per-vertex) "is A a rectangle" check. Takes `dxf_import::LayeredPolygon` directly as input (layer/`.isCircle`/`.children` all already in the right shape). Validated against real drilled profiles from `tests/fixtures/FLAT.dxf` (profiles with 2+ real hole children from the actual polygon tree), not just synthetic single-hole squares |
| `main/nfpDb.ts` (NfpCache key format/eviction) | `nesting::cache` | not started | |

## Phase 3 — Single-threaded placement (first end-to-end milestone)

| Electron file/function | Rust module | Status | Notes / gotchas |
|---|---|---|---|
| `placeParts` greedy per-sheet loop (`main/background.js`) | `nesting::placement::place_parts` | done | Opens sheets once, never revisits (a part that fails the current sheet is deferred to a new one — same as the original, cross-sheet backfill is Phase 5's `refineConsolidation`). Top-left-corner fast path for the first part on a sheet; dominant-part-area threshold (`DEFAULT_DOMINANT_PART_AREA_THRESHOLD = 0.9`) closes a sheet immediately after one part claims most of it; rotation-retry loop (`config.rotations` equal angle steps) preserved exactly, including the part keeping its last-tried rotation across sheets (`parts[i] = r` → `NestPart` replaced in place) |
| `tryPlacePartOnSheet` (batched-holeless-obstacle subtraction, deferred-validation scoring) | `nesting::placement::try_place_part_on_sheet` | done | Real simplification, not a functional change: works directly in plain `Point` coordinates throughout instead of porting `toClipperCoordinates`/`nfpToClipperCoordinates`/`ScaleUpPath`/`toNestCoordinates` — `geometry::clipper`'s boolean-op wrappers already scale internally (`DeepnestScale`) and are true set operations that don't need caller-managed path winding for correctness (same pattern `inner_nfp`'s general fallback already relies on). Batched pending-clips-then-flush for consecutive holeless obstacles, one-at-a-time subtract-then-union-back-in for obstacles with holes (so a later obstacle can still cut into an earlier one's restored hole), deferred overlap validation with retry-against-next-best via `find_best_candidate` (byte-for-byte port of the original's bar-climbing comparison, including the placement-type-independent x tiebreak). New composed helper `geometry::obstacle_nfp::obstacle_nfp` ports `getOuterNfp(A,B,inside=false,config)`'s "A's holes are additional opportunities for B" children logic |
| Three placement-type scorers | `nesting::placement` (inline in `try_place_part_on_sheet`) | done | Gravity (`width*5+height`), box (`width*height`), convex hull (`getHull`/`geometry::hull_polygon::hull`, falls back to the ungrouped point list on hull failure same as the original). **NaN-fitness gap, explicitly resolved**: `minarea`/`minwidth` are `Option<f64>`, only set by the ≥2nd-part scoring branch; `place_parts` uses `.unwrap_or(0.0)` for a sheet with 0-1 parts placed — the same zero-contribution choice the original's `(minwidth\|\|0)/sheetarea + (minarea\|\|0)` made via JS's undefined-is-falsy coercion, now explicit instead of implicit |
| Milestone: one rectangle placed on one sheet, single individual, no GA, no threads, rendered in Tauri shell | `nesting::placement` unit tests | placement logic done, Tauri rendering not started | 5 unit tests: single rectangle top-left placement, two non-overlapping rectangles, oversized-part-gets-penalized-and-left-unplaced, dominant-part-closes-sheet, and box/convexhull placement types. Wiring the result into the Tauri shell for a visual render is Phase 6 scope (`src-tauri` commands don't exist yet) |
| `config.mergeLines` edge-merge fitness bonus (`mergedLength`) | — | deliberately deferred | Optional scoring nicety, not required for core placement correctness; depends on per-point `.exact` marking that isn't tracked on `geometry::Point` yet (only as a parallel `Vec<bool>` inside `simplify_polygon`'s internals). Add both together if/when needed — not blocking Phase 4/5 |

## Phase 4 — Concurrency model + GA orchestration

| Electron file/function | Rust module | Status | Notes / gotchas |
|---|---|---|---|
| `NfpCache` as one shared structure behind a lock | `nesting::cache` | data structure done, **not wired into the placement pipeline yet** | Plain `Mutex<HashMap<String, CachedNfp>>` (the honest default per this row's own note — no contention data yet to justify sharding), keyed by Phase 2's unified `cache_key::nfp_cache_key`, poison-recovering (`.lock().unwrap_or_else(PoisonError::into_inner)`) so one panicking rayon worker thread can't permanently break the cache for every other thread. `CachedNfp` is an explicit `Outer{outer,children} \| Inner(Vec<Vec<Point>>)` enum instead of the original's `Nfp \| Nfp[]` union disambiguated by a caller-supplied `inner` flag. Real simplification found while porting: the original hand-tracks an `entryCount` field solely to avoid `Object.keys(db).length` being O(n) per insert — Rust's `HashMap::len()` is already O(1), so that bookkeeping isn't needed. Clone-on-read/write (required in JS so a live NFP-tracing mutation of `Point.marked` can't corrupt a cached entry another caller later reads) falls out for free from `Point` being `Copy` — ordinary `Vec::clone()` already produces an independent copy, no custom clone method needed. `MAX_CACHE_ENTRIES = 50000` preserved exactly (bounded memory over an OOM crash on a long run). **Gap found by `reviewer.md`'s Phase 4-6 pass**: nothing in `placement.rs`/`dispatch.rs`/`consolidation.rs` actually constructs or consults an `NfpCache` — every real run recomputes every `inner_nfp`/`obstacle_nfp` from scratch, including for identical (part, rotation) pairs that repeat constantly across a GA population/across generations, exactly the redundant work this cache exists to avoid. Wiring it in needs `try_place_part_on_sheet`'s obstacle loop (currently just `placed: &[LayeredPolygon]`, no id/rotation attached) to carry id/rotation alongside geometry — a real signature change through `place_parts` and `consolidation.rs`'s call sites, deliberately not rushed into the bug-fix pass that found this gap. A faithful cache-key match also can't reach full parity with the original's dedup-by-*shape* behavior (JS keys by `.source`, the shape-template index shared by all quantity-copies; this port only has `NestPart::id`, a per-instance id) without adding a shape-identity concept nothing currently tracks — worth deciding deliberately, not as a side effect of wiring the cache in |
| 100ms-poll dispatch loop → `rayon::scope` per generation | `nesting::dispatch` | done | Replaced by construction, not ported 1:1: `rayon`'s `par_iter()` is synchronous (blocks until every item finishes), so there's no polling loop, no `processing` flag, no hand-tracked `running < config.threads` counter at all - `run_generation()` evaluates a whole generation's population in one parallel call, `run()` loops that for N generations tracking the best result via `is_better_nest`. New dependency: `rayon` (already decided in `RUST-REWRITE-PLAN.md`, not a fresh choice). Faithfully preserved despite the redesign: the original's dispatch loop skips any individual whose `.fitness` is already truthy - the only way that happens is `GeneticAlgorithm::generation()`'s elitism carrying the surviving `population[0]`'s already-correct fitness forward unchanged (same genes, so same placement result) - `run_generation` skips those too instead of redundantly re-placing them, a real caught-by-test bug during porting (first draft re-evaluated every individual every generation, wasting a full placement computation on the elite each time) |
| Window-pool-starvation race + failure-recovery paths | — (not ported as code) | superseded | the invariant this guarded against (every individual reaches a terminal outcome) is structurally guaranteed by `rayon::par_iter()`'s blocking semantics - there's no async per-worker dispatch state that could starve or leak in the first place, so there's no separate race to test against |
| Progress/log events: rayon closures → Tauri/tokio event loop | `src-tauri::commands::run_nest_with_progress` | done, at the command layer not `dispatch` | Resolved differently than this row originally anticipated: `nesting::dispatch` still has no callback parameter (each generation is already fully synchronous/blocking by construction, so there's no mid-generation progress to report - the interesting granularity is per-generation, not per-individual). `run_nest_with_progress` (Phase 6) inlines `dispatch::run`'s loop in `src-tauri::commands` instead and calls an `on_progress(generation, generations, best_so_far)` hook after each one; `run_nest_command` supplies a closure that does `app.emit("nest-progress", NestProgressDto {..})`. Needed a `src-tauri/capabilities/default.json` that didn't exist before (`core:event:default`) - custom `#[tauri::command]`s are invokable with zero capability config, but Tauri v2 gates its *own* core/plugin commands (`event.listen`/`.emit` included) behind the ACL system, so the frontend's `event.listen` silently never received anything until this was added (caught by actually running the app and checking the console, not just compiling) |
| `GeneticAlgorithm` class (elite-seed `population[0]`, three mutate operators incl. rotation-reroll rate cap, OX crossover, roulette selection) | `nesting::ga` | done | `main/deepnest.js`. **Real design change, not just a port**: a gene is a part *id* (`usize`), not a reference to the part object — the original's `individual.placement` holds shared object references (cheap in JS, a shallow `.slice(0)`), and `mate`'s crossover containment check only ever compares `.id` anyway, so carrying full `LayeredPolygon` geometry through every mutate/mate call (potentially hundreds of individuals/generation) would've been real, avoidable clone cost. Real part lookup by id stays the caller's job (`nesting::placement::NestPart`). `randomWeightedIndividual`'s `exclude` also changed from JS's `pop.indexOf(exclude)` (reference/position-based already) to an explicit population index for the same reason cleanly — no ambiguity even though ids can repeat. Uses `rand` (new dependency — no existing RNG anywhere in the workspace, and a GA fundamentally needs one; `rand::thread_rng()` also happens to be exactly the right shape for Phase 4's later per-worker-thread rayon dispatch, no locking needed) |
| `widenRotationsIfStalled` / `refineStalledBest` | `nesting::dispatch` (moved from the planned `nesting::ga::widen_rotations`) | not started | `main/deepnest.js`. Both mutate live dispatch-loop state (stagnation counters, in-flight refine requests, shared `config.rotations`) that only exists once the generation-loop dispatcher does — deliberately not built as scaffolding ahead of that; revisit when `nesting::dispatch` lands |
| `isBetterNest` | `nesting::ga::is_better_nest` | done | Simpler than the original: `PlaceResult`'s fields (Phase 3) are always concrete, so there's no `a.unplacedCount \|\| 0`-style undefined-coercion to replicate |
| Rotation-angle-grid quirk as user-facing config + warning | `nesting::ga::config` | not started | see gotchas above; still pending the warning surface, `GaConfig.rotations` itself is already plain user-facing config with no silent correction |
| `tests/ga-seeding.spec.ts` | `nesting::ga` unit tests | done | population-size/elitism/permutation-validity/rotation-grid coverage (11 tests) — not a 1:1 port of the original spec file's exact cases, but the same properties |
| `tests/refine-stagnation.spec.ts` | `nesting::ga` unit tests | not started | blocked on `widenRotationsIfStalled`/`refineStalledBest` above |

## Phase 5 — Shared cache + consolidation

| Electron file/function | Rust module | Status | Notes / gotchas |
|---|---|---|---|
| `refineConsolidation` (sparsest-sheet-first ranking, smallest-part-first ordering fix, 15-target-sheet cap, 2000ms deadline) | `nesting::consolidation::refine_consolidation` + `recompute_totals` | done | `main/background.js`. Deadline/caps kept hardcoded (`MAX_TARGET_SHEETS_TRIED = 15`, 20-iteration cap), no speculative config surface added; `deadline: Instant` replaces the original's `Date.now()`-comparable `deadlineMs` timestamp - same wall-clock budget, Rust's monotonic-clock idiom for it. Reuses `nesting::placement::try_place_part_on_sheet` for the actual relocation attempt (as anticipated in that function's own doc comment when it was written in Phase 3) - doing so surfaced a real latent panic risk in it, fixed here: a relocation target isn't guaranteed to already have a part on it (`place_parts` itself never calls `try_place_part_on_sheet` with an empty `placed`, since the first part on a sheet always takes a separate inline fast path), and the Gravity/Box scoring branch's bounding-box union unconditionally assumed at least one already-placed part. Fixed to fall back to just the candidate's own bounds when nothing is placed yet, instead of relying on a call-site invariant a future caller could easily violate (the original doesn't guard this either - `getPolygonBounds([]).x` on a `null` result would throw - it just never hits the path in real usage, traced and confirmed still true for this caller's exact control flow, but not worth leaving fragile). `sheetsById`/`partsById` simplified to direct `sheets` slice-indexing (`SheetPlacement::sheet_index` is already a stable identity, no separate id-map needed) plus the already-existing `parts_by_id: HashMap<usize, LayeredPolygon>` shape `nesting::dispatch` also uses. `mergedLength` accumulation not ported (see `nesting::placement`'s module doc for why `config.mergeLines` isn't tracked anywhere yet) |
| `tests/refine-consolidation.spec.ts` | `nesting::consolidation` unit tests | done | 4 tests: drains-a-sparse-sheet (the real scenario this function exists for), does-nothing-when-nothing-fits, respects-an-already-passed-deadline, recompute_totals against a hand-built layout - not a 1:1 port of the original spec file's exact cases, but the same properties |

## Phase 6 — Tauri command layer + frontend wiring

| Electron file/function | Rust module | Status | Notes / gotchas |
|---|---|---|---|
| `BACKGROUND_*` IPC channels → Tauri commands + `emit()` events | `src-tauri::commands` (`import_dxf`, `run_nest`) | first slice done | The redesign: the original dispatches one `background-start` IPC message per GA individual to a pool of separate worker `BrowserWindow` processes, collecting `background-response` messages back asynchronously; `run_nest` collapses this to a single synchronous command per nest run, since `nesting::dispatch` already parallelizes a generation in-process via rayon (no separate worker process to message). `import_dxf` reads a file path, parses it (`dxf` crate + `geometry::dxf_import`), returns the parent/hole tree. Both are thin `#[tauri::command]` wrappers around plain, Tauri-runtime-free functions (`import_dxf`/`run_nest` in `commands.rs`) - unit-tested directly, including one against the real `tests/fixtures/FLAT.dxf`. New `src-tauri/src/dto.rs`: an explicit serialization boundary (`PointDto`/`PolygonDto`/`PartDto`/`NestConfigDto`/...) rather than deriving `Serialize`/`Deserialize` on `geometry`/`nesting` types directly - keeps those crates I/O-free as designed. `expand_parts` mirrors `launchWorkers`'s per-quantity id assignment + decreasing-area seed sort. **Not yet covered**: `widenRotationsIfStalled`/`refineStalledBest` (need a run loop that persists across many calls, which this single-shot command isn't), `refine_consolidation` isn't wired into `run_nest` yet either. **New scope beyond the original**: `NestConfigDto` now takes independent `margin` (sheet-edge clearance) and `spacing` (inter-part clearance) fields, both defaulting to `0.0`. The original app has no separate margin concept at all - only `config.spacing`, applied `-0.5*spacing` to the sheet and `+0.5*spacing` to parts (`main/deepnest.js:1188-1205`), which is fine when there's only one clearance value but silently double-counts edge clearance the moment margin and spacing need to differ (found and fixed via `crates/nesting/examples/bench.rs`'s benchmark work - see its git history). `run_nest` now pads sheets/parts internally (`geometry::clearance::prepare_sheet`/`prepare_part`) purely to shape placement decisions; the response still reports placements against the caller's original ids/geometry unchanged, since a placement's `(rotation, x, y)` is a rigid transform valid for the true (unpadded) shape too - see `geometry::clearance`'s module doc for the full derivation. Frontend gained MARGIN/SPACING config inputs to match |
| `frontend/index.html`'s `require("electron").ipcRenderer` (`DeepNest` construction) | new minimal `frontend/index.html` + `app.js` + `app.css` | done, but **replaces rather than adapts** the legacy UI | User decision (asked directly, see conversation): rather than adapt the legacy Ractive UI, write a small new frontend from scratch against exactly what exists today (`import_dxf_command`/`run_nest_command`). Investigating the alternative first showed why: `frontend/ui/index.js`'s `initialize()` alone does 7 `require()` calls at module load (`electron`, `@electron/remote`, `graceful-fs`, `form-data`, `axios`, `path`, `@deepnest/svg-preprocessor`) before anything else runs, none available in Tauri's webview (no Node integration, by design); `config.service.ts`/`preset.service.ts`/`nesting.service.ts`/`nesting-console.ts` (~4700 lines total under `frontend/ui/`) all take an `ipcRenderer` and call `.invoke()`/`.on()`/`.send()` directly; and part of what that UI does (loading *SVG* files from a nest directory) is for a feature this project's DXF-only scope change already dropped. Adapting it would have been a rewrite in substance, just constrained to keep the old structure. **New minimal UI** (dark grey, brutalist, no framework, matches this project's no-bundler convention): `index.html` (import panel → shape role/quantity table → config panel → result panel with per-sheet SVG rendering), `app.js` (calls both commands via `window.__TAURI__.core.invoke`, mirrors `expand_parts`'s id-assignment client-side to map returned placement ids back to shape geometry, corrects DXF's y-up vs SVG's y-down coordinate convention when rendering), `app.css`. Legacy `frontend/deepnest.js`/`svgparser.js`/`ui/**`/`util/**` files are **kept in the tree, unreferenced** - reference/inspiration only, not deleted, not wired to anything. Two small Rust-side changes this needed: `tauri.conf.json`'s `app.withGlobalTauri: true` (so the no-bundler frontend can reach `invoke` without an npm-installed JS binding package) and `#[tauri::command(rename_all = "snake_case")]` on both commands (keeps top-level command-argument casing consistent with the DTOs' already-snake_case nested fields, instead of Tauri's camelCase-args/snake_case-fields split). **Verified end-to-end manually** (no project run-skill existed yet for a Tauri app on Windows; CDP remote debugging via `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS` didn't attach for this WebView2 version, fell back to OS-level screenshot + mouse/keyboard automation): imported the real `tests/fixtures/FLAT.dxf` (99 shapes), assigned one shape as a stand-in SHEET and the rest as PARTs, ran a real nest (population 4, generations 2, ~5s), got a correct result (97 large sheet-sized parts correctly unplaced against a same-sized sheet, 1 small part correctly placed and rendered). **Later addition, same phase**: the shapes table and the result view originally only ever drew a shape's outer boundary (`shape.points`), silently dropping `.children` - real DXF fixtures turned out to already exercise this (`FLAT.dxf` has a `drilling` layer of small holes nested inside several cut profiles), so every child feature on every layer was invisible end to end despite the port having preserved them correctly in the data. Fixed with a recursive `renderShapeSvg(shape, transformPoints)` (same transform function reused for both a shape's own local-bounds thumbnail and a placed part's rotate+translate+sheet-relative view, since children share their parent's rigid transform) and a deterministic name→hue `colorForLayer` hash (no fixed palette makes sense for arbitrary real layer names). Confirmed visually: the `drilling`-layer holes render as small dots in a distinct color from the cut outline, where before they didn't render at all. Also added: a PREVIEW column (small SVG thumbnail per row, same renderer) in the shapes table, and a live CONSOLE panel (`#console-log`) that logs import/run start/success/failure plus a line per generation via the `nest-progress` event above - the "just like the old Electron project" ask. **Further additions, same phase**: multi-file import via `tauri-plugin-dialog`'s native multi-select open dialog (`window.__TAURI__.dialog.open({multiple: true, filters: [...]})`) plus drag-and-drop (`dragDropEnabled: true` in `tauri.conf.json`, `tauri://drag-enter`/`-leave`/`-drop` core window events) - both funnel into one `importPaths(paths)` that appends onto whatever's already imported rather than replacing it. That exposed a real latent bug: `renderShapesTable` used to clear and rebuild the whole `<tbody>` on every call, which silently discarded any ROLE/QTY the user had already set the moment a second file (or a dropped file) came in - fixed by making it append-only (only renders rows past `body.children.length`). Also added a "define a rectangle by size" mini-form (WIDTH/HEIGHT/LAYER NAME + ADD RECTANGLE) so a sheet or part that isn't in any DXF on hand (e.g. a stock size you just want to try) doesn't need one - it pushes a plain 4-point rectangle shaped exactly like an imported `PolygonDto` onto the same `importedShapes` array, so it flows through the existing role/quantity/nest pipeline unchanged. Needed a new `dialog:default` capability grant (same ACL gate as the `core:event:default` one above - the dialog plugin's `open` is a plugin command, gated the same way). **Further additions**: every config field in panel-config (and TOLERANCE) now has a plain-language `title` tooltip explaining what it does. `NestConfigDto` gained `max_threads: usize` (`0` = uncapped, the default) - `run_nest_with_progress` builds a scoped `rayon::ThreadPoolBuilder` and runs the generation loop via `.install(...)` when it's set, rather than touching rayon's global pool (which can only ever be configured once per process). `PlaceResult`/`RunNestResponse` gained `unplaced_ids: Vec<usize>` (just `parts.iter().map(|p| p.id).collect()` on whatever's left in `place_parts` at the end) - the frontend renders each unplaced part's own shape in the "error" red color in a new UNPLACED section on the result panel, with a `title` tooltip giving a best-effort reason (checked independently: does the part's own bounding box fit *any* available sheet's bounding box at all, in either orientation - if not, "too large" rather than "try more generations"). The console panel is now `position: fixed` in the top-right corner with a semi-transparent background (always visible without scrolling, doesn't fully obscure what's behind it), and import/run both show a small CSS spinner plus (for a nest run) a real progress bar driven by the same `nest-progress` events. **Further additions**: the shapes table (ASSIGN ROLES) is now collapsible (a COLLAPSE/EXPAND button next to its heading), since it gets very long for real jobs with 100+ shapes and was pushing CONFIGURE/RESULT far down the page. `RunNestResponse` gained `history: Vec<NestSnapshotDto>` - every genuinely-better nest found during a run (not just the final winner), tracked by cloning `PlaceResult` only on an actual improvement (bounded by how often the GA's elitism gets beaten, not one clone per generation regardless of `generations`). The RESULT panel gained a VIEW ATTEMPT selector populated from it, defaulting to the last (best) entry, so "the other nests it tried" are inspectable, not discarded. **Fixed a real UI freeze**: both commands were plain (non-`async`) `#[tauri::command]`s, which Tauri v2 dispatches on whatever thread handles IPC - on desktop, the same thread that pumps the window's event loop. A long-running one (import of a big DXF, or a nest run with enough generations/parts - reported directly against an 80-generation run) blocked that thread for its whole duration, freezing the entire window: no repaint, no input, nothing, until it returned. Fixed by making both `async fn` and moving the actual work into `tauri::async_runtime::spawn_blocking(...).await` - the event loop thread is now free the whole time, and `nest-progress` events (emitted from the spawned thread, `AppHandle` being `Send`) keep arriving live instead of all queuing up behind a blocked main thread. **Rebrand**: window title (`tauri.conf.json`), `<title>`, and the header `<h1>` wordmark changed from "deepnest"/"DEEPNEST" to "RustyNesting"/"RUSTYNESTING" (project/crate/package names deliberately unchanged - `net.deepnest.rust`, `deepnest-tauri`, the repo folder - this was a UI-only rename). The Windows taskbar/title-bar icon was still the unmodified default Tauri scaffold icon (a blue bubble/circle graphic, never actually deepnest-branded, but still "the old logo" from the user's perspective) - regenerated `src-tauri/icons/*` (all sizes + a hand-built multi-resolution `icon.ico`) as a simple dark-grey-background/lime-"R" mark matching the frontend's own brutalist theme, via `System.Drawing` (no image-gen tool needed for a flat two-color mark) |
| `main/ui/services/config.service.ts` | Tauri command wrapper | not started | near-mechanical, already a thin IPC wrapper; superseded in spirit by the new minimal UI reading nest config directly from its own form (no persisted config file yet) |
| `main/ui/services/preset.service.ts` | Tauri command wrapper | not started | near-mechanical, already a thin IPC wrapper |
| `tests/dispatch-recovery.spec.ts` | re-tested invariant, not ported | not started | extract "every dispatch always terminates" against the new dispatcher |
| Project run-skill for this Tauri app | — | not started | worth generating (`/run-skill-generator`) now that the manual recipe (kill stray `msedgewebview2`/`deepnest-tauri` processes, launch, OS-level screenshot+`SendKeys` for interaction since CDP attach didn't work) has been worked out once by hand |

## Phase 7 — Import/export, dialogs, DXF, crash recovery

| Electron file/function | Rust module | Status | Notes / gotchas |
|---|---|---|---|
| `@electron/remote` dialog/fs calls | Tauri dialog/fs commands | not started | |
| ~~DXF conversion (`converter.deepnest.app` remote service)~~ | — | superseded | scope change: DXF import/export is native via the `dxf` crate (Phase 1/7), not a remote-conversion round-trip. No `reqwest` dependency needed for this |
| DXF export: write nested layout back out, reproducing each part's original layer | `geometry::dxf_export` + `src-tauri::commands::export_dxf`/`export_dxf_command` | done | New scope, not a port (the original app never wrote DXF locally at all). Lays every used sheet out left-to-right in one drawing space (`sheet_spacing` between them, since a DXF file has no separate "sheets"), writes one `LWPOLYLINE` per layer-tagged node (a part's own true outer boundary *and* every child/hole, each on its original layer - confirmed round-tripping the `drilling` layer). `include_sheet_outline` optionally also writes each sheet's own rectangle. Both frontend-exposed knobs the user asked to be prompted for, in the RESULT panel next to a new EXPORT DXF button, using `tauri-plugin-dialog`'s native save dialog. **Real bug found and fixed while building this**: `Drawing::new()`'s default header targets DXF R12, which predates `LWPOLYLINE` entirely (introduced in R2000) - the writer silently drops any entity type unsupported by the target version, so every part written would otherwise vanish with no error at all (an empty `ENTITIES` section, confirmed by writing then re-reading a minimal file before finding this). Fixed by setting `drawing.header.version = AcadVersion::R2000` explicitly. A part that was a true circle on import is written back out as its tessellated polygon approximation, not a true DXF `CIRCLE` entity - a disclosed simplification (visually indistinguishable at normal tolerances), not a bug |
| `main/ui/services/import.service.ts` | Tauri command wrapper | not started | needs rework beyond a mechanical port — it's built entirely around the remote-conversion-server flow (`convertAndImport`), which no longer exists |
| `main/ui/services/export.service.ts` | Tauri command wrapper | not started | |
| Crash recovery (serialize/deserialize imported part geometry to strings) | TBD | not started | pattern already built and tested this session in the Electron repo, for SVG elements — needs adapting to the DXF entity/polygon tree instead |
| `tests/export-recovery.spec.ts` | re-tested invariant, not ported | not started | "always round-trips recovery snapshot" |

## Phase 8 — Remaining UI wiring + benchmark logging

| Electron file/function | Rust module | Status | Notes / gotchas |
|---|---|---|---|
| `main/ui/components/parts-view.ts` | frontend (ported as-is) + Tauri wiring | not started | |
| `main/ui/components/sheet-dialog.ts` | frontend (ported as-is) + Tauri wiring | not started | |
| `main/ui/components/nest-view.ts` | frontend (ported as-is) + Tauri wiring | not started | |
| `main/ui/components/navigation.ts` | frontend (ported as-is) + Tauri wiring | not started | |
| `main/ui/components/nesting-console.ts` | frontend (ported as-is) + Tauri wiring | not started | |
| `main/benchmarkLogger.js` (git-tagged, dual-file, 5MB-rotated CSV logging) | `nesting::benchmark_log` | done | `git_revision()` (`-dirty` suffix from `git status --porcelain`, cached for the process), `append_benchmark_line` (per-generation detail, 5MB-rotated to `.old`), `append_run_summary_row` (per-run CSV, header written once) - matches the original's exact two-file split and `nest-benchmark-runs.csv` column philosophy (one row per run, meant for lining runs up side by side). Built to support `crates/nesting/examples/bench.rs`: a real timed-runs benchmark (N runs x M seconds each, real `tests/fixtures/FLAT.dxf` parts, sheet/margin/spacing as actual job parameters via `geometry::clipper::offset` pre/post-processing - see the example's own doc comment) used to look for performance bottlenecks empirically, same spirit as the original's four tuning sweeps |

## Phase 9 — Parity verification, perf, packaging

| Item | Status | Notes / gotchas |
|---|---|---|
| `nest-cli --fixture X --seed N` headless differential CLI | not started | reuses `benchmarkLogger.js`'s CSV format so both engines land in diffable rows |
| Differential runs vs. Electron app on benchmark fixtures | not started | compare unplaced-count/sheet-count/utilization statistically (GA is stochastic) |
| Rayon core-saturation check vs. 8-window Electron cap | not started | verify, don't assume |
| Tauri packaging/icons/installer | not started | |

## Will not port (verified dead this session)

| Item | Verification |
|---|---|
| `applyPlacement()` (`main/deepnest.js:1775`) | zero call sites repo-wide |
| `overlapTolerance` config field | zero references outside its own default |
| On-disk `./nfpcache` directory + delete-on-quit logic | no writer exists anywhere; already in-memory-only in practice |
| `main/util/simplify.js` | **resolved**: still loaded via `<script>` tag with no static call site in the app's own code, but this no longer matters for the port either way — `deepnest.js`'s `simplifyPolygon` pipeline calls the external `@deepnest/svg-preprocessor` npm package's `simplifyPolygon` in production (identical `(points, tolerance, highQuality)` signature/algorithm), and that's the function `geometry::simplify_polygon` actually ports. `main/util/simplify.js` served correctly as the port's test-oracle reference (via `geometry.spec.ts`) regardless of which copy production calls |

## Dropped from scope (mid-project decision — these are live/used code in the
## Electron app, unlike "will not port" above; they're cut because the input/
## output format changed to DXF-only, not because they're dead)

| Item | Why dropped |
|---|---|
| `main/svgparser.js` (SVG import: DOM → polygon tree, parent/hole detection, `.isCircle`, oversized-part bbox check) | Replaced entirely by DXF import (`geometry::dxf_import`). The same *shape* of work (entities → polygon tree with hole/circle detection) is still being built, just against DXF entities instead of an SVG DOM — see the Phase 1 table above |
| `main/util/domparser.ts` | SVG-DOM-specific helper for `svgparser.js`; no DXF equivalent needed (the `dxf` crate provides a structured entity tree directly, no DOM parsing step) |
| `main/util/matrix.ts` (SVG transform-string parsing) | Only needed for SVG's `transform="matrix(...)"` attribute strings; DXF has no equivalent surface in scope right now |
| DXF-via-remote-conversion-server flow (`converter.deepnest.app`, `main/ui/services/import.service.ts`'s `convertAndImport`) | Replaced by native local DXF parsing (`dxf` crate) — the whole reason for this scope change is that the remote-conversion approach only ever produced SVG output and had no way to preserve DXF layers |
| `main/util/eval.ts` | **Live** (unlike the "will not port" rows above) — verified it's `main/util/parallel.js`'s child-process worker entrypoint (`parallel.js` spawns it and sends serialized code via IPC message for it to `eval()`). Not dropped for being dead; dropped because its only reason to exist is the Electron multi-`BrowserWindow`/child-process worker model, which Phase 4 replaces with rayon (real Rust closures on OS threads, no code-string `eval` or IPC serialization involved) — same rationale as `main/util/parallel.js` itself, already in the reference-only list below |

## Reference-only files (Electron side, not ported, kept for lookup)

- `main.js` — IPC handlers, window pool, single-instance-lock (superseded by Phase 6's Tauri command layer, not ported 1:1)
- `main/util/interact.js`, `ractive.js`, `svgpanzoom.js`, `pathsegpolyfill.js`, `parallel.js` — vendored JS libs, ported to `frontend/util/` as-is (no Rust port; frontend is reused, not rewritten)
- `main/font/**`, `main/img/**`, `main/style.css` — static assets, ported to `frontend/` as-is
- `tests/index.spec.ts` — general test harness scaffolding, not a spec to port 1:1; revisit once `nest-cli` exists
